version: 2
jobs:
  build:
    docker: 
      - image: circleci/node:8.11.3
    steps:
      - checkout 
      - run:
          name: Setup dependencies
          command: |
            npm install
      - run: npm run test
    